You are a fresh Claude Fable 5.1 reviewer validating the last eleven commits on branch nikita/feat/scribe-bootstrap in /home/alfakentavr/scribe before the owner tags v0.1.0. You have no memory of building this code. Treat everything as suspect. You may run read-only git (log, show, diff), `uv run --frozen scribe ...` and `uv run --frozen pytest -q ...` from /home/alfakentavr/scribe. Do not edit source, tests, records or the ledger; do not commit, stage, stash, or touch git state; do not write any file except the report named at the end.

Read first:
1. /home/alfakentavr/scribe/docs/build/04b-codex-validation.md: the original Codex gpt-6-astra findings, in particular V3, V5, V6, V8, V12, V14, V17, V19 and V21.
2. /home/alfakentavr/scribe/docs/build/06-validation-triage.md: the intended fix for each, including the Batch B section with the owner's calls (V6 never delete and CI fails; V12 authority = attested, live, not superseded; V3 refuse a dirty ledger and check the base tip; V8 one ledger lock everywhere; V19 no pending cap; V21 warn on a subdirectory start).
3. /home/alfakentavr/scribe/docs/build/08-codex-review-batch-a.md: Codex's "New defects" and "V1 supervisor" sections; commit 3050f09 claims to close them (deny token, forced exit codes, gate state callbacks, rm rule boundaries).
4. The implementer reports: docs/build/07-batch-a-report.md (group 3 section), 09-batch-b-report.md, 09-batch-b-item-{2,3,4,5}.md, 10-q3-post-rewrite.md, 10-q4-pytest-verify.md, and lines I85 to I127 plus M11 to M14 of /home/alfakentavr/scribe/AUTONOMOUS_DECISIONS_09_08_2026.md.
5. The diffs, in order: `git show 16ae245` (group 3: V5, V6, V14, V17), `git show 3050f09` (review follow-ups and V12), `git show 2b81766` (V21), `git show 9d8cb06` (V3), `git show 6057c02` (V19), `git show 406cced` (V8), `git show 0677c09` (pytest verify engine), `git show b7f4528` (post-rewrite git hook), `git show 3ca275f` (four dogfood records), `git show 27aefbe` (fixture pinning), and the HEAD commit (backlink fold).

For each of V3, V5, V6, V8, V12, V14, V17, V19, V21 and each of Codex's 08-review "New defects" and "V1 supervisor" items, state one of: `closed` (the fix matches the intent and the named test would fail on the old code; check by reading the test, not by trusting the report), `partial` (what is missing), `regressed` (what broke, with a reproduction), or `disagree` (why the fix is the wrong approach). Be specific: file, function, line. Then review the two features (pytest verify engine, post-rewrite hook) for defects with the same precision, paying attention to: the verify subprocess (argument injection through `target`, timeout handling, cwd), the post-rewrite hook re-entry guard and its interaction with post-commit during a rebase (I127), and the ledger lock (deadlock or double-acquire between post-commit, post-rewrite and relink). Then read the four new records in docs/decisions (the D-260909-* files) as a skeptical ratifier: does each state a real decision with honest options, is the evidence quote actually the decisive turn, would you ratify, reject, or ask for a rewrite, and why. Finally list any new defects across all eleven commits, and run the full suite once (`uv run --frozen pytest -q -p no:cacheprovider`, about 150 s) and report the counts. Do not repeat Batch C items (V7 history replay, V13 staged-content checks, V23 fixtures) as defects; they are deferred with records.

Output: write a Markdown report to /home/alfakentavr/scribe/docs/build/11-fable-final-review.md starting with `# Fable 5.1 final review before v0.1.0`, a one-line verdict (`tag`, `fix first`, or `rework`), then `## Batch A group 3`, `## Batch B`, `## 08-review follow-ups`, `## Pytest verify engine`, `## Post-rewrite hook`, `## The four new records`, `## New defects`, `## Suite`. Plain hyphens only, no em or en dashes, no emojis. Under 2000 words. Your final message to the caller is the verdict line plus a five-line summary of the most important findings; the file holds the detail.
