You are finishing the implementation (plan task T16) of `scribe`, a Claude Code plugin in /home/alfakentavr/scribe (git repo, branch nikita/feat/scribe-bootstrap, workspace-write sandbox; .git is read-only for you, so never run git commands that write; read-only git such as log, status, diff, show is fine; the main session commits your work afterwards). Tasks T1 to T15 are done and committed; the suite is green apart from 2 deliberately skipped deferred tests.

Read first:
1. /home/alfakentavr/scribe/docs/build/03-plan-v2.md: section 0.1 (implementer contract), 0.2 (definition of done, 13 items), section 7 item 4 (the two-worktree supersede scenario), and the text of task T16 in section 6.
2. /home/alfakentavr/scribe/docs/build/04-progress.md (what each task did, who did it, what the main session verified).
3. /home/alfakentavr/scribe/AUTONOMOUS_DECISIONS_09_08_2026.md (I-lines from many concurrent subagents, M-lines from the main session, O-lines from the owner).
4. tests/test_deferred.py, README.md, and skim src/scribe/ so the report is accurate.

Do, in order:
A. Add `test_two_worktree_supersede` to tests/test_deferred.py, skip-marked with reason exactly `deferred: two-worktree supersede test, enable before turning any gate to enforce`, whose body contains the full scenario of plan section 7 item 4 as executable code (it must import and parse; it is skipped, not run). `uv run pytest -q` must then print `N passed, 3 skipped`.
B. Add a "Tests" section to README.md: how to run the suite, what the 3 skips are and when to enable each, the timing test and its budgets.
C. Consolidate the `## Step 4 implementer decisions` section of AUTONOMOUS_DECISIONS_09_08_2026.md: keep every line and its number, but if numbering collided because agents appended concurrently, renumber sequentially and add one line at the end of that section saying you renumbered. Do not touch M-lines, O-lines, P-lines or R-lines.
D. Run every item of the definition of done (plan 0.2) and record pass or fail with the observed output. For item 6, run `uv run scribe relink` once first, as section 8 says, then `uv run scribe lookup` for each of the three aliases. For item 13 the bootstrap sha is 3a5b1d2. For item 11 use `rg -n -e $'—' -e $'–' .` and expect no output (rg is available).
E. Write /home/alfakentavr/scribe/docs/build/04-implementation-report.md: what was built, by task, with who built it (Codex gpt-6-astra with a gpt-5.6-sol subagent for T2 and T3, Fable 5.1 or Opus 5 or Sonnet 5 subagents for the rest, per 04-progress.md); test count and the skips; the definition-of-done table from D; every deviation from the plan with its I-line or M-line; the UNVERIFIED items U1 to U3 and the module that isolates each; known gaps; and step 5 reviewer instructions: `claude --plugin-dir /home/alfakentavr/scribe`, then `/reload-plugins`, then try `/scribe:decide`, `/scribe:ratify`, `/scribe:lint`, plus how to check that the PreToolUse injection fired (edit a file under src/scribe/ and look for the retrieval candidates block) and that the git hooks added trailers.
F. Run `uv run pytest -q`, `uv run scribe lint`, `uv run scribe index --check`, `uv run scribe validate docs/decisions` one final time and make sure they are green. You may spawn one gpt-5.6-sol subagent for step A if you want; everything else do yourself.

Rules: uv only, no installs; plain hyphens only, no em or en dashes, no emojis in any file; never edit the three record bodies in docs/decisions/D-*.md, never edit RATIFICATIONS.jsonl by hand; do not edit anything else under docs/build/ except 04-progress.md (mark T16 done with its acceptance result) and 04-implementation-report.md. If a definition-of-done item fails, do not paper over it: fix the code only if the fix is small and obviously correct, otherwise record the failure in the report with what you found.

Finish with one line: `DONE: <N> passed, 3 skipped, DoD <k>/13 pass, report at docs/build/04-implementation-report.md`, or `PARTIAL:` and what remains.
