# Autonomous decisions, 2026-09-08, scribe build pipeline

Mode: autonomous, owner-approved five-step pipeline (`research/decision-scribing/build-brief.md` in the `~/.claude` repo). Each step appends its own numbered items below. Plan items are `P`, review items should be `R`, revision items `V`, implementation items `I`.

## Step 1, planner (Fable 5.1), plan written to `docs/build/01-plan.md`

- P1. Python package `src/scribe/` with console script `scribe` (`uv run scribe ...`), project name `scribe`. Reason: `python -m scribe` and `scribe` read the same in hooks, skills and docs; the `src` layout keeps tests from importing the working copy by accident. Rejected `scribe_core/`: the extra word buys nothing since the package is never published.
- P2. Runtime dependency: PyYAML only. Reason: front matter has nested lists of flow mappings (`history`, `affects`, `verify`); a hand-written subset parser would be the riskiest module in the repo. ULID is implemented in the standard library (48-bit ms timestamp plus 80 random bits, Crockford base32), no `python-ulid` dependency. Dev dependency: pytest.
- P3. `requires-python = ">=3.11"`, `.python-version` pinned to `3.12`. Reason: the workstation has system Python 3.10 and a uv-managed CPython 3.12.14; no 3.11 is installed, so a 3.11 pin would force a download. The git hook shims run under system `python3` (3.10 is enough for an `os.execvp` call).
- P4. Record filename is the alias (`docs/decisions/D-YYMMDD-slug.md`), ULID inside. Reason: humans read filenames and `git log --stat`; a same-alias collision between branches shows up as an add/add merge conflict, which is the visible failure; lint flags `duplicate_alias` if both survive.
- P5. `decided_by` enum simplified to `human | agent-recommended | agent` from README 10.2's `user-ratified | model-recommended, user-accepted | model-default, unreviewed`. Reason: the README values contain commas and spaces, which need quoting in YAML and break `grep`-style indexing; the meaning is unchanged and documented in plan section 3.2. The three hand-written records use `human` as instructed by step 0.
- P6. Commit trailer format `Decision: <alias> <ulid>`, both tokens, space separated. Reason: the alias makes `git log` readable, the ULID is the identity that survives alias collisions; the lookup accepts either token alone for hand-written trailers.
- P7. Scratch state lives at `<worktree>/.claude/scribe/state.json` (README 10.4), not at `docs/decisions/.scratch/` which the bootstrap `.gitignore` anticipates. Reason: README is the design source of truth; `docs/decisions/` should contain only records, the index and the attestation file. `scribe init` appends `.claude/scribe/` to `.gitignore`; the existing `docs/decisions/.scratch/` line is harmless and can be removed by the implementer.
- P8. Ratification attestation: `docs/decisions/RATIFICATIONS.jsonl`, append-only, one line per verdict with the record's body SHA-256, is the "path the agent's deny rules block". A record with `review_state: ratified` and no matching attestation is invalid. Reason: the record file itself must stay agent-writable (the agent writes and updates records), so the human-only path has to be a separate file. Limits: the agent can still run `scribe ratify` via Bash; the CLI tags such entries `via: claude-code` when the `CLAUDECODE` env var is present (UNVERIFIED). Raised as R3 in the plan.
- P9. Phase 3 gates run in shadow mode: they compute the verdict, append it to `.claude/scribe/gate-log.jsonl`, and exit 0. `SCRIBE_GATES=enforce` flips them to exit 2 and only tests set it. Reason: the brief wants them scaffolded fail-open, and the owner wants one-way-door frequency measured before anything heavier (A13); a log gives that number for free.
- P10. `scribe check` (CI) scopes the supersede rule to records added or modified between the base ref and HEAD, not to every record in the tree. Reason: a pre-existing case that reached `main` before branch protection would otherwise fail every PR forever.
- P11. The dropped CI workflow installs scribe with `uvx --from "$SCRIBE_SOURCE"` where `SCRIBE_SOURCE` is a placeholder git URL, because the scribe GitHub repository does not exist and must not be created without asking. Rejected: vendoring a standalone checker script into each target repo (drifts from the package). Open question for the owner: create the repo and tag `v0.1.0`, or prefer vendoring.
- P12. Git hooks are installed as Python shim files (`#!/usr/bin/env python3`, `os.execvp` into `uv run --frozen --project <PLUGIN_ROOT> scribe git-hook <name>`), with the plugin root baked in at `scribe init` time. Reason: brief forbids bash hooks; a shim keeps the hook file tiny and fail-open (missing `uv` prints one line and exits 0). Cost: if the plugin moves, `scribe init --force` must be re-run; the shim carries a `# scribe-managed v1` marker so init can tell its own hooks from foreign ones.
- P13. DEMM grades live in `docs/build/01-records-grading.md`, not in the record bodies. Reason: the body section list is fixed and validated; a grade is review output, not part of the decision.
- P14. `scribe index` is read-only over records (writes only `INDEX.md`); rebuilding `implementation_links` from git trailers is a separate `scribe relink` command. Reason: README 10.4 says "the indexer rebuilds links", but a generator that silently mutates records makes `index --check` in CI a side-effecting step; splitting keeps CI pure.
- P15. `post-commit` marks a record `implemented` and adds a link only when the commit changed paths outside `docs/decisions/`. Reason: the commit that adds the record itself is not its implementation.
- P16. A `SessionStart` hook is added although the brief's phase 2 list does not name it. Reason: `prepare-commit-msg` needs a session id in scratch state before the first prompt-triggered write, and the first `uv run` after install is slow; SessionStart pays that cost with a 120 s timeout so edit-path hooks stay under 1 s. It also prints the review-queue count as context when non-zero.
- P17. `verify` entries (grep engine) are executed by `scribe lint` only, not by `commit-msg` or `scribe check` in this run. Reason: `commit-msg` is warn-only and time-sensitive; running greps over globs there adds latency for a warning nobody acts on yet. README 10.4 schedules them in commit-msg and CI; deferred, logged.
- P18. The validator requires at least one blockquote line in `## Evidence` (the verbatim decisive turn, A12) and an `## Options considered` table with exactly one `chosen` row. Reason: these are the two properties DEMM grades as "who decided" and "what was rejected"; making them structural keeps agent-written records from omitting them.
- P19. PreToolUse injection is emitted as `{"hookSpecificOutput": {"hookEventName": "PreToolUse", "additionalContext": ...}}`. UNVERIFIED against the brief's plugin facts; fallback is plain stdout. Isolated in one module so a fix is one edit.
- P20. The three hand-written records are `review_state: ratified`, `ratified_by: "@nikita"`, with attestation lines `via: hand-written` in `RATIFICATIONS.jsonl`, body hashes computed by the planner. Reason: the owner made these decisions in the interview; writing them as unreviewed would misstate who decided. Timestamps `2026-09-08T20:37:41Z` to `:43Z` match the ULID timestamp prefixes (system clock at generation).
- P21. `affects` entries of `type: package` are accepted by the validator but ignored by the edit-time matcher in this run; a third type `action` is reserved for the Bash denylist (plan 4.7). Reason: only path matching has a consumer today.
- P22. Hook stdin field names (`session_id`, `cwd`, `tool_input.file_path`, `tool_input.command`, `stop_hook_active`, `last_assistant_message`, `prompt_id`) are used as commonly documented but marked UNVERIFIED in the plan because the brief's verified facts do not list them. Every read is tolerant of absence.
- P23. Injection budget: at most 5 records, 220 characters per line, 1600 characters per block; internal deadline 700 ms; hooks.json timeout 3 s for edit-path hooks. Reason: README asks for "budget bounded, under 1 second"; the numbers are starting points for step 5 to measure.
- P24. The plan has 16 tasks (T1 to T16), ordered so `uv run pytest` is green after each; T10 (git hooks) is the only L-sized task. Reason: git hooks touch three entrypoints and real git in tests; splitting them would leave the repo in a state where trailers are added but never consumed.
- P25. `commit-msg` honours `SCRIBE_COMMIT_MSG=enforce` already (exit 1 on unknown or invalid decision references) although this run ships it warn-only. Reason: the flip to enforce should be a config change later, not new code.

## Open questions for the owner (batched, non-blocking)

- Q1. Create the `scribe` GitHub repository and tag `v0.1.0` so `scribe-check.yml` can install it via `uvx --from git+...`, or prefer vendoring a checker into each repo? (P11)
- Q2. Add `Bash(*scribe ratify*)` and `Bash(*scribe reject*)` deny rules in target repos? It would also block the human-typed `/scribe:ratify` skill if slash commands pass through the same permission check (UNVERIFIED). (P8, plan R3)
- Q3. Install a `post-rewrite` git hook in a later run so amend and rebase relink automatically, or keep `scribe relink` manual? (plan R5)
- Q4. Add `engine: pytest` to the `verify` allowlist so behavioural decisions can point at a test node? (grading file)

## Owner requests received during the build (main session, 2026-09-08)

- O1. After the pipeline finishes, create two or three toy playground repositories, each already initialised with scribe (`/scribe:init` run, hooks installed, plugin loaded), with a small ready-made change staged in each (for example: add a feature, fix a bug, refactor a module) so the owner only has to fill in the decision record form by hand and commit. Owner words: "the next step for me would be to try filling out those decisions by hand but in some test repos that you create as a playground for me. Like adding a featue, fising a bugm etc. So everything is done, I jsut need to fil out the form. Like a toy exmaples 2-3". Scheduled as step 6, after the step 5 review.

## Main session decisions (Fable 5.1, step 0)

- M1. The step 4 Codex orchestrator commits after each finished task with plain `git commit` in the commit skill's format (type: Title, one-sentence body, `Decision:` trailer where a task implements a record). The owner's rule "always use /commit" binds Claude sessions; Codex is a different tool and has no access to the skill. Per-task commits were chosen over one big commit at the end because Codex runs in 9-minute slices and a slice can die mid-write; a commit boundary per task is what makes "re-run the task, never patch by hand" safe. Nothing is pushed. The main session's own commits (pipeline files, review) go through /commit.
- M2. Pipeline scripts and prompts live in `docs/build/pipeline/` inside the scribe repo rather than the job tmp dir, so the handoff chain is auditable from git alone and the worktree hook (which rejects complex inline commands) is satisfied by `bash <script>`.

## Step 3 reviser decisions (Fable 5.1), plan v2 written to `docs/build/03-plan-v2.md`

- R1. Supersession edge is effective only while the successor is not rejected and its `effective_state` is `proposed`, `implemented` or `superseded`; a shared `reconcile_supersession` marks and restores predecessors (new history event `restored`) from `scribe new`, `ratify`, `reject` and `lint --expire`, while `scribe index` stays read-only. Reason: F1 needed one rule that both the indexer and the injection hook apply, and P14's pure indexer was worth keeping.
- R2. Repeated `ratify` or `reject` with the same verdict is an idempotent exit 0 (v1 said exit 1), and reversals in both directions are allowed. Reason: the F8 recovery path (attestation written, record write crashed) is healed by re-running the same command, which only works if repeats are not errors.
- R3. Attestation line is written before the record change and is the authority; a record left `unreviewed` behind an attestation validates as `state_behind_attestation`. Reason: the file the deny rule protects should never lag the record, and a retry heals the other direction for free.
- R4. Ratification authority in this release is by construction (`disable-model-invocation: true`, the skill's inline `!` command, an anchored `Edit` deny rule, an explicit `--via`), not by proof; `CLAUDECODE` detection dropped; no `Bash(*scribe ratify*)` deny rule. Reason: the skills doc says a matching deny rule aborts skill-injected commands too, so that rule would break the human path, and no documented signal identifies a Claude Code subprocess. Q5 added.
- R5. `SCRIBE_GATES` and `SCRIBE_COMMIT_MSG` move from environment variables to `.claude/scribe/config.json` (git-ignored, `version: 1`), keeping their names. Reason: F21's inherited-environment risk, and keeping the name keeps record A13's `verify` entry (`SCRIBE_GATES` in `pre_tool_use_gate.py`) truthful without touching the record.
- R6. `body_sha256` pinned to `rstrip()` of the text after the closing `---` line (CRLF normalized); front-matter YAML comments are not preserved on save. Reason: only the `rstrip` definition reproduces the three committed attestation hashes (checked with a script); PyYAML cannot round-trip comments and the comments are not keys.
- R7. Edit-path hook `timeout` set to 1 second in `hooks.json`, internal deadline 700 ms kept, timing test measures the whole `uv run` subprocess (warm median under 1.0 s, bytecode-cold printed and bounded at 2.0 s) and fails rather than skips without `uv`. Reason: the docs state a timed-out PreToolUse hook is cancelled and the call proceeds, so the timeout is the mechanical fail-open ceiling the brief asks for; a fresh-venv cold start is not measured because it needs network.
- R8. Implementation links are recorded only for changed paths matching the record's `affects` (all non-`docs/decisions/` paths when `affects` has no path entries), one function `links.implementation_paths` shared by `post-commit`, `relink` and `scribe check`. Reason: F3 and F11 both needed a definition of "this commit implements this record", and without it the T1 bootstrap commit (which touches `docs/build/`) would have counted as implementing all three records.
- R9. `scribe init` writes the CI workflow only with `--ci-source <path or URL>`; `.` means the repository is the scribe checkout (dogfood). Reason: F6; vendoring a checker rejected as in P11; withholding keeps every PR from failing on a placeholder.
- R10. `UserPromptSubmit` extracts tracker ids from the prompt text into `task_refs` and `scribe new` consumes them; Claude Code task identity (`TaskCreated`) is deferred as a non-goal. Reason: F10 asked for task correlation or an explicit deferral; prompt-text extraction is the cheapest thing that fills `task_refs` from scratch state as README 10.2 describes.
- R11. Skill commands are written unquoted (`--project ${CLAUDE_PLUGIN_ROOT}`) so they match their `allowed-tools` rule textually; a plugin path with spaces is a listed Windows caveat. Reason: the skills doc shows exactly this pattern for prompt-free execution; the owner's Ubuntu path has no spaces.
- R12. `jsonpath` verify engine rejected with a dedicated `unsupported_engine` message rather than implemented. Reason: F25; no consumer needs it this run and README 10.2 lists it, so the message must say it is deferred, not unknown.
- R13. Title limit 200 characters. Reason: F4, A13's title is 163 characters and the body is immutable; 200 leaves room without inviting paragraphs.
- R14. Minimum Claude Code version cited as 2.1.228 (Edit deny rule also blocking Write), tested locally on 2.1.265; `astral-sh/setup-uv@v10` (latest release v10.0.1 per `gh api`). Reason: F30.
- R15. `marketplace.json` carries the owner's email in `owner.email` because the plugins reference marks it required. Reason: validation would fail on a placeholder; it is attribution in the owner's own manifest. Owner may change it (Q6).
- R16. Task count stays 16; gates (T10) moved before lint (T13), a bootstrap commit added to T1, and every task lists exact dependency edges so the orchestrator can run up to three independent tasks in parallel. Reason: F18 and F23.
- R17. Windows shim uses `#!/usr/bin/env python` and `subprocess.call` instead of `os.execvp` when generated on Windows. Reason: F28; `python3.exe` is absent from python.org installers and `execvp` does not replace the process on Windows.

## Open questions for the owner (batched, non-blocking), continued

- Q5. Ratification is human-triggered by construction (skills the model cannot invoke, inline command, deny rule on the file) but not provable: an agent could still run `uv run scribe ratify` through Bash and the attestation would only show `via: cli`. Accept for now, or invest later in a stronger mechanism (a separate reviewer identity, signed attestations)? (R4)
- Q6. `.claude-plugin/marketplace.json` needs `owner.email`; the plan uses `bognik3@gmail.com`. Keep, or use another address before the repository is ever published? (R15)

## Step 4 implementer decisions

- I1. Prepare and test the T1 skeleton while leaving T1 incomplete when the bootstrap trailer commit fails on the sandbox's read-only .git mount: preserve useful reviewable work, record the unmet commit requirement, and do not start dependent tasks until the commit is possible.
- M3. Codex slice 1 was blocked by its sandbox: `.git` is read-only under workspace-write, and network is off so `uv lock` could not fetch pytest and PyYAML. Fix: the orchestrator no longer commits; it appends a commit queue to `docs/build/04-progress.md` and the main session commits between slices through /commit (this also restores the owner's "always /commit" rule, superseding the M1 exception). Network inside the sandbox is enabled with `-c sandbox_workspace_write.network_access=true`, scoped to these runs only, no global Codex config change. T1's bootstrap trailer commit is made by the main session as an empty commit because the records were already committed in 3a5b1d2 without trailers.
- M4. Claude Code's low-memory guard killed two orchestrator slices within a minute of launch (the machine shows 1.7 GB free but 16 GB available, no swap; Chrome holds most of the used memory). Codex slices now run through `codex_resume_detached.sh` (setsid plus nohup, outside the harness's process tree) with the same 20-minute hard cap, and the main session waits on a done-marker file. The cap and the `< /dev/null` stdin rule are unchanged.
- M5. Codex hit the ChatGPT usage limit at 18:18 EDT during T3 (retry allowed at 21:21). The owner chose "Switch to Claude agents now" over waiting or buying credits. From T4 on, the main Fable 5.1 session orchestrates one Claude subagent per plan task (same plan v2 text, same acceptance commands, main session re-runs tests and commits through /commit). The gpt-6 validation pass of the code from the original org chart is therefore not performed; step 5 review by the main session stands in for it. T3's 39 task tests and acceptance command were run by the main session after Codex died and pass.
- M6. The first Claude wave (T4, T5, T6 on Fable 5.1) died at 18:5x EDT on the Claude session rate limit before writing anything. Relaunched at 19:21 with T4 and T5 on Opus 5 and T6 (hook mechanics, unverified Claude Code facts) on Fable 5.1, to spread quota across models; later waves follow the same split: Fable for hook-facing or L tasks, Opus for the rest.
- I2. `scribe lookup <sha>` resolves every whitespace token of every `Decision:` trailer independently, dedupes the resulting record lines and prints `UNKNOWN <token>` for each unresolved token: this is the literal reading of section 4.6 item 1, and it keeps a half-valid trailer (good alias, garbage ULID) both resolved and reported.
- I3. `scribe lookup <alias-or-ulid>` prints the record path, then a `commits:` section and an `implementation_links:` section, each entry indented two spaces, with `  (none)` when a section is empty: section 4.6 item 2 fixes the content but not the layout, and labelled sections keep the two lists apart without parsing.
- I4. A commit with no `Decision:` trailer exits 0 with `no Decision trailers on <sha>` rather than 1: section 4.6 reserves exit 1 for unknown tokens and unresolvable arguments, and the commit itself was found.
- I5. INDEX.md review-queue lines carry the same space-joined `affects` field as Active lines, and any empty field is dropped instead of being rendered as an empty cell: rule 4 of section 3.5 omits `affects` only on Retired lines, the section 3.5 example simply shows queue records that have none, and `| |` reads as a broken line.
- I6. Retired lines print the title alone, with no `Regret:` or `Review` tail: that is the shape of the section 3.5 example, and a retired record's regret condition and review date are no longer actionable.
- I7. The title tail joins title, `Regret: <regret_when>` and `Review <date>` with `. ` after stripping one trailing period from each segment: the real records' `regret_when` values already end in a period and the plan's example line shows exactly one.
- I8. Active and Retired are ordered by date descending then alias, the key section 3.5 fixes for the review queue, and `scribe index` takes an optional store path and prints `wrote|unchanged <path>, <n> records` while `--check` prints `up to date` or `out of date, run scribe index`: rule 5 demands deterministic output and the plan fixes no text for the command itself.
- I9. Hook stdin fixtures carry `"cwd": "."` and the launcher resolves a relative `cwd` against the process cwd: Claude Code sends an absolute path, but a checked-in fixture cannot name a machine path and the T6 acceptance commands run from the repository root; tests override `cwd` with the tmp repo.
- I10. SessionStart and UserPromptSubmit write no scratch state when the repository has no `docs/decisions/` store (plan 4.1 item 8 applied before 4.3 step 1): otherwise SessionStart would create `.claude/scribe/state.json` in every repository opened with the plugin loaded.
- I11. `src/scribe/__main__.py` calls `sys.exit(main())` instead of `main()`: `python -m scribe hook gate` must carry exit 2 in enforce mode, and the `run_hook` test fixture runs hooks through `-m scribe` rather than the console script.
- I12. Each hook module declares `POLICY = "advisory" | "gate"` (or `select_policy(payload)` for the mixed reconcile event) and `launcher.dispatch` chooses `run_advisory` or `run_gate` from it, with `gate` and `reconcile` shipping as advisory no-op stubs: T7 and T10 then change only their own module, and no allow verdict is logged for every Bash call before the real gate exists.
- I13. The lock adapter's platform switch is the module attribute `state.LOCK_PLATFORM` (default `os.name`) rather than `os.name` read directly: the fake-`msvcrt` test cannot patch `os.name` to `nt` without `pathlib` refusing to instantiate paths on Linux.
- I14. The injection line's `ratified by <who>` prints the record's `decided_by` value, not the `ratified_by` handle: the section 4.4 example reads `ratified by human` for a record whose `ratified_by` is `@nikita`, and `decided_by` is the attribution the reader needs at edit time.
- I15. The 1600-character block cap is met by dropping records from the end of the ordered list until the block fits, and a single oversized record is hard-truncated with `...`: section 4.4 fixes the cap but not the trimming, and keeping the highest-ranked records whole beats cutting every line.
- I16. Within a review group the sort is implemented, proposed, backtracked, then date descending, then alias: section 4.4 item 7 only orders implemented before proposed, and backtracked records are the least likely to still govern an edit.
- I17. The deadline anchor `_START` is stamped at import of `pre_tool_use_edit`, which under `launcher.dispatch` happens after stdin is read, so the 700 ms budget excludes interpreter and uv startup: that is the literal wording of section 4.1 item 7; in-process tests reset the anchor through an autouse fixture because pytest imports the module at collection time.
- I18. `tests/test_timing.py` generates its 100 records as unreviewed clones of a real record, each carrying a `src/scribe/*.py` pattern plus one negated pattern: the timed run then exercises the full path (100+ candidates, matching, sorting, the 5-record cap) instead of a no-match early exit.
- I19. The record template lives at `src/scribe/templates/record_template.md`, not at a top-level `templates/`: section 2 puts it inside the package and A12's `verify` entry greps that exact path, so a top-level copy would fail lint.
- I20. Template placeholders are `{{NAME}}` tokens replaced by plain string substitution, matching the `{{SCRIBE_CHECK_COMMAND}}` convention section 5.4 already uses: no template engine and no `str.format`, so a body containing braces cannot break rendering.
- I21. The `by` of the `proposed` history entry falls back to `scribe-new` when neither `--by` nor `provenance.agent` is set: section 4.9 names only those two sources and the schema requires `by` to be a string.
- I22. `provenance.session` takes the spec value when present, otherwise `--session`: section 4.9 fixes `--session` only for the history entry, and a spec that names its own session is the more specific statement.
- I23. `scribe new` rejects unknown spec keys before generating anything: a mistyped spec key would otherwise be dropped silently and the record would ship with a default its author never chose.
- I24. `--register` without `--session` registers under the session id `unknown`: section 4.9 names that bucket, and skipping the registration instead would lose the ULID that `prepare-commit-msg` needs.
- I25. The gate and reconcile handlers return no verdict, and so write no gate-log line, for payloads outside their scope (a tool other than Bash, PowerShell or ExitPlanMode; a hook event other than TaskCompleted or Stop; a cwd without a docs/decisions store; a TaskCompleted while no decision_worthy flag is set): plan 4.7 item 4 logs every verdict for the two gate tools, but an allow line for every TaskCompleted in every session would be noise, and the T6 stub test expects silence for foreign payloads.
- I26. ExitPlanMode areas are the eight README 10.4 policy-area names (dependency, public-api, schema, storage, concurrency, safety, interface, deviation) with the plan's eleven keywords mapped onto them (add package to dependency, migration to schema, thread to concurrency, deviat to deviation), matched case-insensitively as substrings: the plan fixes the keywords but not the area names, and the scratch-state example in 4.2 shows area names rather than keywords.
- I27. decision_worthy.prompt_id falls back to the session's most recent prompt_ids entry when the ExitPlanMode payload carries no prompt_id: plan 4.1 item 3 says the field is absent before the first prompt, and the last recorded prompt is the best pointer available.
- I28. The publish and upload denylist rules also match pnpm, yarn, npm.cmd and npm.exe, and http-write also accepts --request <method> and -X<method> without a space, while the rule names stay exactly the plan's eighteen: these are the spellings PowerShell and curl users type, and a rule that misses them would under-report in shadow mode.
- I29. `Record.apply_change` gains a keyword-only `force` flag that appends a history entry even when the value is unchanged, and ratify and reject pass it for the three verdict fields: the section 3.8 matrix promises three entries per reversal, and without it a person reversing their own verdict would lose the `ratified_by` entry because the value stays the same.
- I30. The idempotent `already ratified` and `already rejected` path still runs `reconcile_supersession` and regenerates INDEX.md, appending no attestation and no history on the record itself: section 4.10 recovery says the next ratify or reject heals a stale index, and the T9 acceptance line requires a plain re-run to heal an injected index-write failure.
- I31. Healing `state_behind_attestation` writes the re-run's `--by` and timestamp into the record fields and appends no line whenever the latest attestation already matches the requested verdict and the current body hash: the attestation stays the authority, and reusing the original line's timestamp could violate the non-decreasing history rule.
- I32. `scribe ratify` and `scribe reject` accept trailing free words as the note and join them after `--note` when both are given: the skills' argument hint is `<alias-or-ulid> [note]` and `$ARGUMENTS` arrives as bare tokens after the alias.
- I33. `--by` with no value and no git `user.name` falls back to `@unknown`: the schema requires `ratified_by` to be a string and section 4.10 names no fallback.
- I34. Any `OSError` inside the ratify order is caught by the CLI and printed as `scribe <verb> failed: <exc>; re-run the same command to finish` with exit 1: the partial state is designed to be healed by a re-run, and a traceback would hide that instruction.
- I35. The `description` of the ratify and reject skills is a quoted YAML string: the plan's wording contains `: `, which a strict YAML parser rejects inside a plain scalar; the text itself is unchanged.
- I36. The `link_added` and `implemented` history entries written by post-commit go through `Record.apply_change`, so `link_added` carries `field: implementation_links` with the whole old and new lists plus `commit`, not the bare `new: {commit, paths}` of the section 3.3 example: section 3.3 also names `apply_change` as the only code path that appends history, the schema accepts both shapes, and one appender beats a second one.
- I37. `prepare-commit-msg` selects pending ids with `links.implementation_paths` being non-empty rather than a raw "some staged path matches `affects`" test: it is the same predicate post-commit uses to link and consume, so a trailer added from pending state is always consumed by the commit that carries it (F3) and paths under `docs/decisions/` never count.
- I38. On amend (`source == commit` with a sha) the staged paths are diffed against the amended commit's parent (the empty tree for a root commit) instead of HEAD: HEAD is the commit being amended, so diffing against it hides the amended content and a decision made pending after the original commit could never earn its trailer (F14, AT d2).
- I39. `commit-msg` check 1 reports a trailer with no resolvable token as `unknown decision: <value>` and a trailer whose tokens resolve only partly or to different records as `alias/ulid mismatch in trailer: <value>`; both are fatal under enforce: section 5.2 lists check 1 among the enforced checks and names only those two conditions.
- I40. The governed-path check (F12) defines "active record" as INDEX.md does (review_state not rejected, effective_state in proposed, implemented, backtracked, no effective incoming edge): section 5.2 says "active" without defining it and section 3.5 is the one place the plan does.
- I41. `scribe git-hook` catches every exception in a hook, appends it to `hook-errors.log` (stderr too under `SCRIBE_DEBUG=1`) and exits 0, while `commit-msg` under enforce returns 1 as a value: section 5 says entrypoints exit 0 on every failure and section 5.2 says enforce exits 1, so only the deliberate path is non-zero. Candidate origins (`record-carried`, `pending`) are printed to stderr only under `SCRIBE_DEBUG=1`, the switch 4.1 item 6 reserves for extra stderr.
- I42. `scribe check` requires `--base` and prints `unknown base ref: <ref>` with exit 1 when the ref does not resolve: section 5.4 never shows the command without a base, and guessing `origin/main` inside CI would silently check the wrong range.
- I43. The revisions the check reads with `git show` are the merge base of `--base` and HEAD, not the ref itself: section 5.4 fixes the range as `<base>...HEAD`, so the append-only and immutability rules compare against the same fork point the diff uses.
- I44. Rule 2 fails the check on validate errors only and lets warnings through: `scribe validate` itself exits 1 only on errors, and a warning such as `superseded_without_successor` is something a legitimate branch can carry.
- I45. A record present at the base but deleted in HEAD produces no finding: none of the five rules in section 5.4 covers deletion, and reporting `immutable_changed` for a file that is gone would name the wrong problem.
- I46. The check prints one reason per line as `<path>: <code>: <message>`, with the supersede-gate sentence kept verbatim and unprefixed: section 5.4 fixes only that sentence, and a code in every other line lets CI grep for `immutable_changed` or `index_stale`.
- I47. The append-only rule compares `RATIFICATIONS.jsonl` line by line rather than as a byte prefix: the unit of the ledger is the JSON line, and a base file whose last line lost its trailing newline would otherwise read as a rewrite.
- I48. The range-oriented git plumbing (`merge_base`, `diff_names`, `rev_list_range`, `show_blob`, `tree_record_paths`) lives in `gitutil.py`, not in `history_check.py`: T13 lint reuses `history_check` against its own base ref and should not reach git through it.
- I49. The shim template `githook_shim.py` and the workflow template `scribe-check.yml` live in `src/scribe/templates/` next to the record template, rendered by `{{NAME}}` string replacement: I19 puts templates inside the package and I20 fixes the placeholder convention, and the plan's `templates/` paths in T14 are read as that directory.
- I50. One shim template serves both platforms: the shebang is chosen at init time (`python3` on POSIX, `python` on Windows, F28) while the `os.execvp` versus `subprocess.call` switch is a runtime `os.name` branch inside the shim carrying the U3 `UNVERIFIED` comment: a single template cannot drift between two copies, and the Windows arm stays isolated for the owner's smoke test.
- I51. A `--ci-source` spec that is neither a `git+https://` or `https://` URL nor an existing path exits 1 before any write, and a bare `.` renders `--project .` with no warning: section 5.4 defines only those forms, and writing a workflow around a typo would fail every pull request.
- I52. A workflow file that exists without the `# scribe-managed` marker is kept with exit 0 and replaced under `--force` without a backup: 5.5 item 7 says skip unless forced, item 10 reserves exit 1 for foreign hooks and preflight, and a tracked workflow keeps its history in git, unlike an untracked hook.
- I53. `unchanged` means the file already holds the exact text init would write, so a managed hook whose baked-in plugin root differs (plugin moved) is rewritten and reported as `wrote`; `.claude/settings.json` is also `skipped` with a message when `permissions` or `permissions.deny` has the wrong type, the same treatment 5.5 item 8 gives invalid JSON; `docs/decisions` reports `unchanged` when present and the `next steps` block lists only files that exist.
- I54. `scribe lint` prints `<path>: <severity>: <code>: <message>` and a closing `N records, E errors, W warnings, I info`: section 4.12 fixes the codes and severities but not the format, and matching `scribe validate` keeps one output shape across the two commands.
- I55. A `verify` entry with `expect: match` whose `paths` select no file at all is `verify_failed` at the entry's own severity, not a silent pass: a deleted target is exactly the drift the entry exists to catch, and reporting it as `verify_error` would claim the entry itself is broken.
- I56. `--fix-index` regenerates `INDEX.md` and reports `index_stale` as `info` rather than dropping the finding: section 4.12 says it regenerates instead of erroring, and a silent rewrite would hide a change the owner still has to commit.
- I57. An explicit `--base` that does not resolve is the error `unknown_base` (exit 1), while the default chain (`origin/main`, then `HEAD~1`, then skip) stays silent: a typo in a flag the caller typed is a mistake, a repository with one commit is not.
- I58. `--expire` still prints the `proposal_stale` warning for each record it expires, with `, expired by --expire` appended: the run has to say which records it changed, and inventing a second code for the same rule would leave the table in section 4.12 incomplete.
- I59. The file list the `verify` globs match against is the whole working tree minus `.git/`, built once per run: section 4.12 names only that exclusion, and pruning `.venv/` or `__pycache__` as well would make a record's `verify` result depend on which directories happen to exist locally.
- I60. Two records sharing an `id` are reported under `duplicate_alias`, the same code as two sharing an `alias`: section 4.12 gives that one code for both halves of the rule, and the message names which key collided.
- I61. `scribe relink` rebuilds `implementation_links` purely from `Decision:` trailers found by walking `git rev-list --all`, not from the existing link list plus a reachability check on trailer-less commits: section 5.3 names the trailer as the one source of truth for which commit implements which record, and a link that lost its trailer (history rewritten by hand) is exactly the kind of drift relink exists to correct.
- I62. `tests/test_relink.py` commits its `Decision:` trailers directly with `git commit -m` instead of installing the git hook shims T11 uses: relink reads trailers straight from `git log`, independent of `prepare-commit-msg`/`post-commit`, so exercising it does not need the hook plumbing, and it keeps this file's fixtures self-contained rather than importing helpers out of `tests/test_githooks.py` (no `tests/__init__.py`, so cross-file imports would rely on pytest's rootdir sys.path insertion instead of a package import).
- I63. `mark_implemented` after a relink uses the last entry of the rebuilt `implementation_links` list as the commit recorded on the `implemented` history entry: section 5.3 says only that a proposed record gains `implemented` on its first link, and the plan does not name which commit a multi-link relink should cite.
- O2. Owner (20:0x EDT): once the Codex quota resets at 21:21, run the gpt-6-astra validation pass of the original org chart as a read-only code review over the finished repository (`docs/build/04b-codex-validation.md`) and fold it into the step 5 report. Owner words: "I think we might get a codex reset by that time too" and "yeah, do that".
- M7. Owner (20:3x EDT) asked to rely on Sonnet 5 for implementation and Opus for review because subagent usage is near the limit. Applied from T15 on (T13 on Opus and T14 on Fable were already running); step 5 review agents run on Opus.
- M8. Owner (20:3x EDT): once Codex resets at 21:21, delegate remaining subagent work to Codex. Agreed split: T15 on Sonnet 5 now (no idle gap), then Codex gpt-6-astra runs T16 (deferred two-worktree test, final sweep, implementation report) and the read-only validation pass (O2).
- M9. Step 6 playgrounds (O1) built by a Sonnet 5 subagent at `~/scribe-playground/` (01-add-feature, 02-fix-bug, 03-refactor with a ratified seed record to supersede), each with scribe init applied, a prepared uncommitted change, TASK.md, a pre-filled decision.json and a `playground-start` tag. Two workflow observations recorded in the TASK.md files: post-commit writes links back into the record after the commit, so a follow-up commit is needed; `scribe check --base main` only bites from a feature branch, so repo 03 has the owner branch first.
- M10 (2026-09-09). Codex V1 fixed with a supervisor instead of changing uv usage: `hooks/supervise.py` (stdlib only, system `python3`, 3.8+) runs `uv run --frozen --project <plugin> scribe ...`, forwards stdout, stderr and exit code on success, maps any uv startup failure or application crash to exit 0 with one stderr line, and lets a non-zero exit through only when stderr carries the `[scribe-deny]` marker line that `scribe.protocol.announce_deny` prints for a deliberate refusal (gate enforce, commit-msg enforce). hooks.json now registers `python3 -I ${CLAUDE_PLUGIN_ROOT}/hooks/supervise.py hook <event>` (plan 4.1's literal `command: uv` is superseded; DoD item 8 reads "python3 plus the supervisor" from now on). The git shim template imports the same supervisor, so `os.execvp` and the Windows `subprocess.call` branch are gone. Cost: about 30 ms of interpreter startup per hook (warm median 0.56 s, limit 1.0 s) and a Windows dependency on a `python3` command name for the Claude Code hooks (U3 widened). Dogfood shims in this repo refreshed with `scribe init --force`; the live headless test passed again through the new registration.
- I64. T16 consolidation found I1-I63 already sequential and unique, so every existing line and number is preserved with no renumbering; all M, O, P and R lines remain untouched.
- I65. T16 uses `UV_CACHE_DIR=/tmp/scribe-uv-cache UV_OFFLINE=1` for uv commands because the default cache is read-only in this sandbox; the existing environment and cache suffice without installs, and the main session retains commit ownership under M3.
- I66. T16 wraps the timing test's two prints in `capsys.disabled()` because the first green `uv run pytest -q` captured both measurements, failing the output clause of DoD 1; the subprocess measurements and budgets remain unchanged.
- I67. T16 quotes the decide skill's `${CLAUDE_SESSION_ID}` argument to match DoD 9 literally and keep it one shell argument; the plugin-root and allowed-tools convention remains as specified.
- I68. DoD 11 finds both prohibited dash characters in the pre-existing `docs/build/pipeline/step4c_prompt.txt:13`; T16 records the failure without editing that file because the owner explicitly restricts docs/build writes to progress and implementation report.
- I69. The final relink implementation retains reachable existing links and refreshes paths before adding trailer-derived links, as plan 5.3 and 04-progress.md T15 specify; I61 describes an earlier incompatible choice and is retained verbatim for the audit trail, not treated as the final behaviour.
- I70. The required dogfood relink exposed 11 tests whose tmp_repo fixture copied implemented states and foreign commit links from the live ledger; reset only the temporary copies to proposed, empty implementation_links and history without implementation events, preserving bodies and ratifications, so each fresh test repository keeps its intended pre-implementation baseline without requiring bootstrap git history in shallow CI checkouts.
- I71. Historical main-session verification adjustment from 04-progress.md T11: two amend tests pin GIT_COMMITTER_DATE so a same-second amend produces a distinct commit SHA; this is test determinism, not a git-hook behaviour change.
- I72. T16 runs directly in Codex gpt-6-astra without a subagent, following the owner's task-specific instruction that delegation for A is optional and all other work must be done here; this supersedes plan 0.1's one-subagent-per-task workflow for T16 only.
- I73. T16 runs relink once before the three lookup checks, moving the plan section 8 step-5 dogfood mutation into the final implementation sweep at the owner's explicit instruction; generated front matter and INDEX.md are left for the main session to commit, and record bodies and attestations stay unchanged.
- I74. Batch A follows the owner's three group boundaries and optional gpt-5.6-sol item delegation, with the parent running each full suite and writing the commit queue in 07-batch-a-report.md; this replaces plan 0.1's original task and commit workflow for this run because the checkout's git metadata is read-only.
- I75. V7 compares the entire body after the existing line-ending normalization, including trailing whitespace; attestation hashing keeps its specified rstrip normalization because hash equivalence does not grant permission to edit immutable body content.
- I76. V11 gate events always carry matched_rules and uncovered_rules in RULES order, while the existing singular denial sentence names the first uncovered rule; the rm rule scans every target within the same shell segment so a later outside target cannot escape the check.
- I77. V9 retains the shared lock context and requires callers to honor its acquired flag: ratify returns a retryable exit 1 before resolving or writing a record, and scratch updates return the current disk state without running the mutation or writing unlocked. The old test_posix_contention_is_bounded_and_state_still_written asserted the defect and is replaced by test_posix_contention_drops_state_update.
- I78. V10 supersedes I31: recovery copies the attestation's actor and verdict timestamp into the record and uses the rerun's timestamp and actor for recovery history. Idempotence requires agreement on verdict, actor, timestamp and body hash; a latest opposite verdict requires a new attestation for the requested reversal before success can be reported.
- I79. V22 maps attestation verdicts to CLI verbs ratify and reject in recovery guidance. The old schema and ratification tests asserted nonexistent commands, so those assertions are corrected and the healing test now parses and runs the actual diagnostic for both verdicts.
- I80. V19 substitutes template tokens in a single regex pass over the original template; inserted evidence is never rescanned. Session defaults apply only to absent keys, preserving explicit empty lists and letting explicit invalid values reach validation; the pending-decision cap remains unchanged as required by Batch A scope.
- I81. V20 uses a ReachableCommits object per command run to snapshot git rev-list --all and cache abbreviation resolution through rev-parse. Relink uses the same object for existing links and trailer discovery, preserving first-seen link order after resolution so short aliases cannot disappear or create duplicates.
- I82. V15 uses direct os.path.realpath for root and target: existing symlink ancestors resolve while nonexistent suffixes remain, and symlink/.. follows filesystem semantics before containment is checked. A cross-drive relpath ValueError means outside the repository.
- I83. V18 streams bytes through the closing front-matter delimiter and decodes only that prefix, keeping even undecodable bodies out of injection work. A final deadline check follows matching and formatting; the block reserves space for intact record lines, footer and links, then truncates the target to the remaining budget, including limits shorter than the ellipsis.
- I84. V16 tracks implementing records, newly linked records and changed records separately: only changed records need saves, but every implementing commit retries index regeneration and pending consumption. The existing backlink notice still counts only newly added links, so recovery does not claim duplicate backlinks.
- I85. Batch A group 3 covers V5, V14 and V17; V6 is its own section under Batch B's owner call ("never, CI fails"). Both run inline in this session rather than through a Codex or Sonnet subagent, since the owner's task assignment for this run names the implementer directly.
- I86. V17 adds a shared `_in_str_set(value, options)` helper in schema.py and routes every enum-style membership test through it (review_state, effective_state, decided_by, reversibility, blast_radius, provenance.authored_by, provenance.trigger, affects type, verify expect and severity, history event and field) instead of fixing only the reported `review_state: []` case: every one of those checks does `value not in <set of strings>`, which raises TypeError for the same reason on an unhashable value, and the plan's own wording ("type-checks values before membership") reads as the general rule, not the one reproduction.
- I87. V17 gives the ULID-date-overflow diagnostic its own code, `ulid_date_out_of_range`, distinct from `ulid_date_mismatch`: the two failure modes need different fixes (a genuinely wrong date versus a date the calendar cannot represent), and collapsing them into one code would make the message misleading for the second case.
- I88. V17's `Store.records()` now skips a malformed file and logs it (reusing `state.log_hook_error`), but `scribe check`'s own `_validate_all_records` still loads each file directly rather than through `store.records()`: the CI gate must keep reporting a malformed record as its own `parse_error` finding, and routing it through the now-resilient store would silently drop that record from the check instead of failing it. `store.records()`'s resilience is for the *other* consumers (supersede gate, index, lint, gates) where one bad neighbour must not blank out unrelated valid records.
- I89. V5's structural ledger check lives in store.py as `attestation_line_problems`, next to `latest_attestation` which does the same JSONL parsing for lookups; `latest_attestation` itself keeps its best-effort skip-on-malformed-line behaviour (callers expect a dict or None), and the new function is what actually reports every malformed, incomplete or truncated line as an error. It is wired into two places: `scribe check` (as a new reason category) and `ratify.apply_verdict` (refusing to append when the current file already has a problem, before doing anything else under the lock).
- I90. V5 also required `scribe check` to validate every current record rather than only the ones the range's diff touched, because an append to RATIFICATIONS.jsonl can make an otherwise-unchanged record's review state contradict its latest attestation. `_validate_changed_records` and its helper `_changed_record_paths` are removed in favour of `_validate_all_records`, which iterates `store.path.glob("D-*.md")` directly (see I88 for why not through `store.records()`).
- I91. V14 fixes every pathname-returning gitutil helper (`staged_paths`, `staged_paths_against`, `commit_changed_paths`, `diff_names`, `tree_record_paths`) to request `-z` output and split on NUL through one shared `_paths_from_z` helper, instead of patching the backslash-replacement line by line: all five helpers had the identical defect for the identical reason (git's default C-style quoting of unusual bytes), so one decoder is the correct shape, not five independent fixes.
- I92. The pre-existing `test_git_utilities_normalize_git_results` in test_store.py asserted the backslash-to-forward-slash conversion this fix removes (a fake response with a literal backslash in a filename, asserted to come back with a forward slash); per the standing instruction to fix a test that encodes the defect, it now feeds `-z`/NUL-delimited fake output and asserts the literal backslash survives unchanged.
- I93. V6's `record_deleted` check compares `git ls-tree` output at the merge-base fork point against HEAD, not the literal supplied `--base`: every other historical rule in check.py (`_supersede_gate`, `check_attestations_append_only`, `check_records_against_base`) already reads from the fork point for the same reason (a base branch that moved forward independently must not make this branch responsible for someone else's deletion), and V6 has no stated reason to be the exception. This supersedes I45, which said no rule in section 5.4 covers deletion; the owner's call ("never, CI fails") makes it rule 6, with no `--allow-delete` escape.
- I94. `record_deleted`'s alias comes from the base-ref content of the deleted file (parsed front matter), falling back to the filename stem only if that parse fails: the message is for a human fixing a CI failure, and naming the record by its actual alias is more useful than the file's bare stem, which is normally the same text but is not guaranteed to be.
- I95. A renamed record (old path deleted, new path added, `alias` field unchanged) is deliberately not special-cased into a single "renamed" finding: `record_deleted` still fires for the old path (nothing in this session tells `scribe check` that the two paths are the same record), and the new path separately fails its own `alias_filename_mismatch` validation from `_validate_all_records` since its filename stem no longer matches the alias inside it. Both findings are correct on their own terms; recognizing a rename as one event would need a content-similarity heuristic the plan does not specify, so it is left as two ordinary failures per the task's explicit instruction to only state this behaviour, not fix it into one.
- I96. The decide skill (skills/decide/SKILL.md) is left unchanged for V6: it already says "Never edit or delete the earlier record" under "Replacing an existing decision," and it does not otherwise enumerate `scribe check` failure codes the way skills/lint/SKILL.md enumerates lint codes, so there is no failure-code list there to extend. Only the README's `check --base <ref>` CLI-reference row gets the new sentence, per the task's own scoping ("if it lists check failures").
